[build]
builder = "nixpacks"
watchPaths = ["apps/api/**"]

[phases.setup]
nixPkgs = ["python3", "nodejs", "pnpm"]

[phases.install]
cmds = ["pnpm i --frozen-lockfile", "python3 -m pip install -r apps/api/requirements.txt"]

[phases.build]
cmds = ["python3 -m playwright install chromium --with-deps"]

[deploy]
startCommand = "cd apps/api && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
